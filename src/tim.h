#ifndef TIM

#define TIM

#include <stdio.h>
#include <stdlib.h>
#include <string.h> 

int sample(int input);
void calculate_deterministic_probabilities(double* probabilities, int asset_count);


#endif
